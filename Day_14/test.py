from game_data import data

array_length = len(data)

print("Number of elements in the array:", array_length)


first_element = data[0]
last_element = data[49]

# first_element['name']
print(first_element['name'])
print(last_element)
